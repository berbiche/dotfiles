for_window [app_id="^launcher$"] \
  floating enable; \
  border none;

for_window [app_id="xfce4-appfinder"] \
  floating enable; \
  border none;

for_window [instance="xfce4-appfinder"] \
  floating enable; \
  border none;

for_window [app_id="blueman-manager"] \
  floating enable; \
  border none; \
  scratchpad move; \
  scratchpad show;

for_window [app_id="org.gnome.Nautilus"] \
  floating enable;

for_window [title="feh.*/Pictures/screenshots/.*"] \
  floating enable;

for_window [app_id="firefox" title="Developer Tools*"] \
  floating enable;
