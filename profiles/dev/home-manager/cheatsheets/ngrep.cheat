% ngrep, work

# Ngreps on port 8083 for incoming traffic
sudo ngrep -d lo -W byline -q '' port 8083

% ngrep, work

# Ngreps on port 8084 for the matching HTTP X-User-Agent header
sudo ngrep -dlo -Wbyline -qi 'X-User-Agent:' 'dst port 8084'
